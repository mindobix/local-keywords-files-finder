# Root path for the crawler to watch. Defaults to $HOME if not set.
WATCH_PATH=/Users/yourname

# Port for the API server
API_PORT=3001

# Set to 'true' to enable verbose crawler logging
DEBUG_CRAWLER=false
